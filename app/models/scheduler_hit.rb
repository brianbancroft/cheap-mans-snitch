class SchedulerHit < ApplicationRecord
  def self.check_intervals
    # Sort Into unique environements

    ## For each environment, get events.

    ## If time > 3x interval, send warning

    ## Skip warning if warning already sent
  end
end
